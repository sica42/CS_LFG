name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Create addon zip
        run: |
          rm -rf build
          mkdir -p build/CS_LFG

          rsync -av \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='.gitignore' \
            --exclude='.vscode' \
            --exclude='.editorconfig' \
            --exclude='build' \
            ./ build/CS_LFG/

          cd build
          zip -r CS_LFG.zip CS_LFG

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: build/CS_LFG.zip